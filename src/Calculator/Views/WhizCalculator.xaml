<UserControl x:Class="CalculatorApp.Views.WhizCalculator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:CalculatorApp.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">

    <Grid x:Name="WhizCalculatorGrid" Margin="16,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"
                           MinHeight="{StaticResource HamburgerHeight}"
                           MaxHeight="52"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <muxc:WebView2 x:Name="MathSolverWebView2"
                       Grid.Row="1"
                       Source="https://mathsolver.microsoft.com/en"/>
    </Grid>
</UserControl>
