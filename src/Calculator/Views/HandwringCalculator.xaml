<UserControl x:Class="CalculatorApp.Views.HandwringCalculator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:CalculatorApp.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:uc="using:MyScriptEditor.UserControls"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">

    <Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"
                               MinHeight="{StaticResource HamburgerHeight}"
                               MaxHeight="52"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <local:HandwritingEditor x:Name="Editor"
                                     Grid.Row="1"
                                     Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                     EditorBarVisibility="Visible"/>
        </Grid>
    </Grid>
</UserControl>
