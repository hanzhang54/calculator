<UserControl x:Class="CalculatorApp.Views.NoteCalculator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:CalculatorApp.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">

    <Grid x:Name="NoteCalculatorGrid" Margin="16,0,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"
                           MinHeight="{StaticResource HamburgerHeight}"
                           MaxHeight="52"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="CanvasGrid"
              Grid.Row="1"
              Background="White"
              BorderBrush="Gray"
              BorderThickness="1"
              CornerRadius="{ThemeResource ControlCornerRadius}">
            <InkCanvas x:Name="NoteCanvas"/>
        </Grid>
    </Grid>
</UserControl>
